<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Цвет при нажатии -->
    <item android:state_pressed="true" android:color="@color/main_purple" />
    <!-- Цвет, когда выбрано -->
    <item android:state_checked="true" android:color="@color/main_purple" />
    <!-- Цвет по умолчанию -->
    <item android:color="?attr/colorOnBackground" />
</selector>